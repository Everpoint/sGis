{
  "name": "sgis",
  "version": "0.3.2",
  "description": "",
  "main": "dist/sGis.js",
  "types": "dist/sGis.d.ts",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "npm run build-ts && npm run rollup",
    "doc": "npm run build-doc && npm run build-samples && npm run copy-samples",
    "version": "node ./config/set_version.js && git add source/sGis.ts",
    "test": "jest --no-cache",
    "build-doc": "typedoc --out doc/ source/ --excludePrivate --theme doc-theme",
    "build-ts": "tsc --listEmittedFiles",
    "build-samples": "tsc -p ./config/tsconfig-samples.json && node ./config/build-samples.js",
    "copy-samples": "ncp ./temp/samples ./doc/samples && ncp ./temp/source ./doc/source",
    "rollup": "tsc -p ./config/tsconfig-rollup.json && rollup --config ./config/rollup.config.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Everpoint/sGis"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^21.1.2",
    "canvas-prebuilt": "^1.6.0",
    "jest": "^21.2.1",
    "ncp": "^2.0.0",
    "rollup": "^0.50.0",
    "ts-jest": "^21.0.1",
    "typedoc": "https://github.com/Maximkaaa/typedoc#dev",
    "typescript": "^2.5.2"
  },
  "jest": {
    "transform": {
      "^.+\\.tsx?$": "<rootDir>/node_modules/ts-jest/preprocessor.js"
    },
    "testMatch": [
      "<rootDir>/test/**/*.ts"
    ],
    "roots": [
      "<rootDir>/source/",
      "<rootDir>/test/"
    ],
    "moduleFileExtensions": [
      "ts",
      "js"
    ],
    "globals": {
      "ts-jest": {
        "tsConfigFile": "./config/tsconfig-jest.json"
      }
    }
  }
}
